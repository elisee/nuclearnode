header
  button#ToggleMenuButton
  .AppInfo
    a.AppTitle(href="/")= config.title
    if channel
      |  / 
      span.ChannelName= channel.name
      span.ChannelUsers

  .PlayerInfo
    span.PlayerName=user.displayName
    |  
    if user.isGuest
      button#LogInButton= i18n.t('nuclearnode:login')
    else
      a(href=config.hubBaseURL + '/logout?redirect=/apps/' + config.appId + (channel ? '/' + channel.name : ''))#LogOutLink= i18n.t('nuclearnode:logout')
